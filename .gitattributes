# Les scripts shell DOIVENT rester en LF, sinon le shebang casse dans les conteneurs
# Linux (« exec ./x.sh: no such file or directory » — cf. crash du backend Docker sous Windows).
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
